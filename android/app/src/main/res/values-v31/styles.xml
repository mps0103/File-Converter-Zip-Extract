<resources>
    <!--
        Android 12 and up always draw their own splash from the launcher icon before
        the activity appears. Left unconfigured it borrows windowBackground, which
        here already contained the icon, so the icon was drawn once by the system and
        again by the window underneath it.

        These attributes point the system splash at the same icon and colour the JS
        splash uses, and flatten the window behind it to a plain colour, so the whole
        launch reads as one continuous screen.
    -->
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:statusBarColor">@color/splash_bg</item>
        <item name="android:navigationBarColor">@color/splash_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowSplashScreenBackground">@color/splash_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_bg</item>
    </style>
</resources>
