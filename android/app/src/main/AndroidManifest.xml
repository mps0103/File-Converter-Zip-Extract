<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Ads and Play Billing need the network. Conversion itself never does. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Only needed to write into Downloads on Android 9 and older. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Lets the "Open file" button find a viewer without asking for package visibility. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <!-- enableOnBackInvokedCallback: targetSdk 36 turns predictive back on by default,
         which sends the gesture to OnBackInvokedCallback and stops calling the
         onBackPressed that React Navigation still listens on. Without this, back
         would leave the app instead of going up a screen. -->
    <application
        android:name=".MainApplication"
        android:enableOnBackInvokedCallback="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                "Open with" for archives. Two filters, because file managers are
                inconsistent: the first matches providers that report a real archive
                MIME type, the second catches the ones that fall back to
                application/octet-stream and only the file name gives the format away.
            -->
            <intent-filter android:label="Extract archive">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/x-7z-compressed" />
                <data android:mimeType="application/vnd.rar" />
                <data android:mimeType="application/x-rar-compressed" />
                <data android:mimeType="application/x-tar" />
                <data android:mimeType="application/gzip" />
                <data android:mimeType="application/x-gzip" />
                <data android:mimeType="application/x-bzip2" />
                <data android:mimeType="application/x-xz" />
            </intent-filter>

            <intent-filter android:label="Extract archive">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\.zip" />
                <data android:pathPattern=".*\.7z" />
                <data android:pathPattern=".*\.rar" />
                <data android:pathPattern=".*\.tar" />
                <data android:pathPattern=".*\.gz" />
                <data android:pathPattern=".*\.tgz" />
                <data android:pathPattern=".*\.bz2" />
                <data android:pathPattern=".*\.xz" />
                <data android:pathPattern=".*\.ZIP" />
                <data android:pathPattern=".*\.7Z" />
                <data android:pathPattern=".*\.RAR" />
                <data android:pathPattern=".*\.TAR" />
                <data android:pathPattern=".*\.GZ" />
                <data android:pathPattern=".*\.TGZ" />
                <data android:pathPattern=".*\.BZ2" />
                <data android:pathPattern=".*\.XZ" />
            </intent-filter>

            <!--
                "Open with" for everything the viewer reads. Same two-filter shape as
                the archive pair above: one on real MIME types, one on the file name
                for the providers that report application/octet-stream for anything
                they do not recognise.
            -->
            <intent-filter android:label="View file">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="application/msword" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
                <data android:mimeType="application/vnd.ms-excel" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
                <data android:mimeType="application/vnd.ms-powerpoint" />
                <data android:mimeType="text/csv" />
                <data android:mimeType="text/comma-separated-values" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="text/html" />
            </intent-filter>

            <intent-filter android:label="View file">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\.pdf" />
                <data android:pathPattern=".*\.docx" />
                <data android:pathPattern=".*\.doc" />
                <data android:pathPattern=".*\.xlsx" />
                <data android:pathPattern=".*\.xls" />
                <data android:pathPattern=".*\.pptx" />
                <data android:pathPattern=".*\.ppt" />
                <data android:pathPattern=".*\.csv" />
                <data android:pathPattern=".*\.txt" />
                <data android:pathPattern=".*\.html" />
                <data android:pathPattern=".*\.htm" />
                <data android:pathPattern=".*\.PDF" />
                <data android:pathPattern=".*\.DOCX" />
                <data android:pathPattern=".*\.DOC" />
                <data android:pathPattern=".*\.XLSX" />
                <data android:pathPattern=".*\.XLS" />
                <data android:pathPattern=".*\.PPTX" />
                <data android:pathPattern=".*\.PPT" />
                <data android:pathPattern=".*\.CSV" />
                <data android:pathPattern=".*\.TXT" />
                <data android:pathPattern=".*\.HTML" />
                <data android:pathPattern=".*\.HTM" />
            </intent-filter>
        </activity>

        <!-- The real AdMob application id. Unit ids live in src/services/ads.ts,
             which uses Google's test units in debug builds so nothing here can
             generate traffic against the real ones. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2904788540387890~2300166132"
            tools:replace="android:value" />

        <!-- Ads are never requested before the first screen is drawn. -->
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="true"
            tools:replace="android:value" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
