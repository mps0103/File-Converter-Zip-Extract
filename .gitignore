node_modules/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/*.keystore
*.jks
.env
npm-debug.log
yarn-error.log
.DS_Store

# Build output from `react-native bundle`, written in so the debug apk runs
# without Metro. Regenerated on demand and 13MB, so it is not source.
android/app/src/main/assets/index.android.bundle

# Images the bundler copies out of node_modules when --assets-dest is used.
android/app/src/main/res/drawable-*/node_modules_*

# Kotlin compiler scratch output.
android/.kotlin/

# Real signing passwords. Only keystore.properties.example is committed.
android/keystore.properties
android/keystores/
